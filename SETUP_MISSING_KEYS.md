# دليل تظبيط المفاتيح والـ Indexes الناقصة

مكتوب خطوة بخطوة. مش محتاج أي خلفية تقنية — كل خطوة فيها إنت رايح فين
وبتضغط إيه وبتحط القيمة فين بالظبط.

---

## ⚠️ اقرا ده الأول — تصحيح مهم

في تقرير الأوديت قلت إن `AGENT_TASKS_SECRET` و `EYE_EXPERT_SECRET` مش
موجودين وإن الـ endpoints واقفة. **ده مش دقيق.**

اللي حصل: أنا فحصت ملف `.env` اللي على جهازك، ولوج `bahr_agent.log` اللي
بيتكتب من التشغيل **المحلي**. الاتنين دول مالهمش علاقة بالإنتاج.

ولقيت في [`docs/knowledge_layer_design.md`](docs/knowledge_layer_design.md)
إن الموضوع ده **اتقفل بالفعل يوم 2026-08-04**:

> ✅ فجوة الأمان 1 اتقفلت بدليل production: أحمد أكد المتغيرين موجودين في
> Railway، والفحص الإمبريقي المباشر على الدومين الحي أكد: التلات endpoints
> بيردوا 401 بدون توكن وبتوكن غلط، و/health بيرد 200 كـ baseline.

**يعني على الأغلب السرّين دول شغالين في الإنتاج خلاص**، والـ 503 اللي شفته
في اللوج كان من تشغيل آدم على جهازك بس.

نفس الكلام ممكن ينطبق على `GITHUB_TOKEN` — مفيش سطر "Backup" في اللوج
المحلي، بس ده طبيعي لأن الـ backup بيشتغل الساعة 2 صباحًا والجهاز بتاعك
غالبًا مقفول ساعتها. **مقدرش أتأكد من الإنتاج من هنا.**

### 👉 يبقى ابدأ من هنا: شوف إيه المتظبط فعلاً

1. افتح [railway.app](https://railway.app) وسجّل دخول
2. اضغط على المشروع بتاع آدم
3. اضغط على الـ service (المربع اللي فيه اسم المشروع)
4. من فوق اختار تاب **Variables**

هتشوف ليستة بكل المتغيرات. **صوّرها أو اكتبها** وقارنها بالأربعة دول:

| المتغير | موجود؟ | تعمل إيه |
|---|---|---|
| `GITHUB_TOKEN` | ⬜ | لو ناقص → القسم 1 |
| `GITHUB_REPO` | ⬜ | لو ناقص → القسم 1 |
| `AGENT_TASKS_SECRET` | ⬜ | لو ناقص → القسم 2 |
| `EYE_EXPERT_SECRET` | ⬜ | لو ناقص → القسم 2 |

**اللي موجود سيبه زي ما هو خالص.** لو غيّرت قيمة سر موجود، هتكسر Make.com
اللي بيستخدمه (شوف التحذير في القسم 2).

---

## القسم 1 — النسخ الاحتياطي (`GITHUB_TOKEN` + `GITHUB_REPO`)

ده اللي بيخلي آدم يرفع نسخة من بياناته كل يوم على GitHub. من غيره مفيش
نسخ احتياطي بيتعمل خالص.

### خطوة 1‑أ: اعمل مكان يتحفظ فيه النسخ

1. افتح [github.com/new](https://github.com/new) (سجّل دخول لو طلب منك)
2. في خانة **Repository name** اكتب: `adam-backups`
3. **مهم جدًا:** اختار **Private** مش Public — دي بياناتك المالية والعملاء
4. سيب باقي الاختيارات زي ما هي واضغط **Create repository**

خلاص. الاسم الكامل بتاعه هيبقى `bahr9/adam-backups` — ده هو `GITHUB_REPO`.

> لو الريبو ده موجود عندك خلاص، عدّي الخطوة دي.

### خطوة 1‑ب: اعمل مفتاح يسمح لآدم يكتب فيه

المفتاح ده اسمه Personal Access Token — زي مفتاح شقة بتديه لحد معيّن.

1. افتح [github.com/settings/personal-access-tokens/new](https://github.com/settings/personal-access-tokens/new)
2. **Token name**: اكتب `adam-backups`
3. **Expiration**: اختار `90 days`
   - ⚠️ بعد 90 يوم هيبطل ولازم تعمل واحد جديد. لو مش عايز تفتكر، اختار
     `No expiration` — أقل أمانًا شوية بس أبسط.
4. **Repository access**: اختار **Only select repositories** → ومن القايمة
   اختار `adam-backups`
   - ده معناه إن المفتاح ده ميقدرش يلمس أي ريبو تاني عندك. مهم.
5. انزل لـ **Permissions** → **Repository permissions**
6. دوّر على سطر **Contents** وغيّر الاختيار من `No access` لـ
   **Read and write**
   - ده الصلاحية الوحيدة المطلوبة. سيب كل الباقي `No access`.
7. انزل تحت خالص واضغط **Generate token**
8. هيظهرلك نص طويل بيبدأ بـ `github_pat_...`

> 🔴 **انسخه دلوقتي حالًا.** GitHub بيوريهولك مرة واحدة بس. لو قفلت الصفحة
> من غير ما تنسخه، مش هتقدر تشوفه تاني ولازم تعمل واحد جديد.

### خطوة 1‑ج: حط القيمتين في Railway

1. ارجع لـ Railway → المشروع → الـ service → تاب **Variables**
2. اضغط **+ New Variable**
3. في خانة الاسم اكتب: `GITHUB_TOKEN`
4. في خانة القيمة الصق النص اللي نسخته (`github_pat_...`)
5. اضغط **Add**
6. كرر تاني: اسم `GITHUB_REPO` وقيمته `bahr9/adam-backups`
7. Railway هيعمل redeploy لوحده. استنى دقيقة لحد ما يخلص.

### خطوة 1‑د: حطهم على جهازك كمان (اختياري)

ده بس لو عايز تجرب الـ backup محليًا. لو مش محتاج، عدّي.

1. في مجلد المشروع `C:\Users\gowaida\Desktop\adam-v1` افتح ملف `.env`
   بـ Notepad
2. ضيف في آخر الملف سطرين جداد:

```
GITHUB_TOKEN=github_pat_الصق_المفتاح_هنا
GITHUB_REPO=bahr9/adam-backups
```

3. احفظ واقفل

> ملف `.env` ده على جهازك بس ومش بيتنشر على GitHub أبدًا (موجود في
> `.gitignore`). ده مقصود ومظبوط.

---

## القسم 2 — أسرار الـ endpoints (`AGENT_TASKS_SECRET` + `EYE_EXPERT_SECRET`)

### 🛑 قف واقرا ده الأول

الاتنين دول **مش مفاتيح بتجيبها من حد** — دي كلمات سر إنت بتخترعها. آدم
بيستخدمها عشان يتأكد إن اللي بيكلّم الـ endpoints بتاعته هو Make.com مش
حد غريب من الإنترنت.

**لو الاتنين موجودين في Railway خلاص (وده الأرجح حسب التوثيق) — متلمسهمش
خالص وعدّي للقسم 3.**

السبب: القيمة دي محفوظة في مكانين لازم يتطابقوا — في Railway، وفي إعدادات
Make.com. لو غيّرتها في Railway بس، Make.com هيبقى بيبعت القيمة القديمة
وآدم هيرفضه بـ 401، وتسجيل عين الخبير هيقف. **تغيير من غير داعي بيكسر
حاجة شغالة.**

### لو واحد منهم فعلاً ناقص:

#### خطوة 2‑أ: ولّد قيمة عشوائية

افتح PowerShell على جهازك والصق الأمر ده واضغط Enter:

```bash
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

هيطلعلك نص عشوائي طويل. ده هو السر. ولّد **واحد لكل متغير** (يعني شغّل
الأمر مرتين لو محتاج الاتنين) — متستخدمش نفس القيمة للاتنين.

#### خطوة 2‑ب: حطه في Railway

نفس خطوات القسم 1‑ج: **Variables** → **+ New Variable** → الاسم
(`AGENT_TASKS_SECRET` أو `EYE_EXPERT_SECRET`) → القيمة → **Add**

#### خطوة 2‑ج: حطه في Make.com كمان ⚠️

**الخطوة دي إجبارية وإلا الـ endpoint هيرفض كل الطلبات.**

1. افتح [make.com](https://make.com) وسجّل دخول
2. افتح الـ scenario اللي بيكلّم آدم (اللي فيه HTTP request على دومين آدم)
3. اضغط على موديول الـ **HTTP** جواه
4. دوّر على **Headers** وشوف فيه header اسمه `X-Secret-Token`
   - لو موجود: غيّر قيمته للسر الجديد
   - لو مش موجود: اضغط **Add item**، الاسم `X-Secret-Token` والقيمة هي السر
5. احفظ الـ scenario

> آدم بيقبل السر بطريقتين: في الـ header ده، أو في آخر الرابط كـ
> `?token=...`. **استخدم الـ header** — الرابط بيتسجل في لوجات كتير على
> الطريق، والـ header لأ.

---

## القسم 3 — الـ 3 Composite Indexes في Firestore

دي أسهل حاجة في الدليل كله. الـ index زي فهرس الكتاب: من غيره Firestore
مش عارف يرتب النتايج، فبيرفض الاستعلام أو بيرجع نتايج عشوائية.

### الخطوات (بتتكرر 3 مرات)

1. افتح ملف [`FIRESTORE_INDEXES_REQUIRED.md`](FIRESTORE_INDEXES_REQUIRED.md)
2. اضغط على اللينك الطويل بتاع **index رقم 1**
3. هيفتحلك Firebase Console — سجّل دخول بحساب Google بتاع
   `bahr-designs-office` لو طلب منك
4. هتلاقي صفحة **"Create a composite index"** والحقول **متملية جاهزة**
   — متغيّرش أي حاجة فيها
5. اضغط زرار **Create Index** (أو **إنشاء فهرس**)
6. هتشوف حالته **Building** — سيبها وارجع للملف
7. كرر نفس الخطوات مع اللينك التاني والتالت

### بعد كده

الـ 3 indexes هياخدوا من دقيقتين لعشر دقايق يخلصوا (حسب حجم البيانات).
تقدر تتابعهم من [Firebase Console → Firestore → Indexes](https://console.firebase.google.com/project/bahr-designs-office/firestore/indexes).

أول ما حالتهم تبقى **Enabled** بدل Building — خلاص.

**اللي هيتصلح لما يخلصوا:**
- البحث في الملاحظات (`search_memory_notes`) — واقف من 21 يوليو
- جلب الملاحظات (`list_memory_notes`)
- تصنيف تعارض الأقساط هيبقى مبني على آخر حدث فعلاً مش عيّنة عشوائية
- تحذيرات التعارض الوهمية في الحالة الذاتية هتقل

---

## القسم 4 — إزاي تتأكد إن كل حاجة اشتغلت

### الـ backup

من تليجرام، ابعت لآدم:

> شغّل الباك أب دلوقتي

المفروض يرد برسالة فيها ✅ وأسماء الـ collections وعدد المستندات في كل
واحدة. لو رد بـ ⚠️ أو ❌، ابعتلي الرسالة.

وبعدها اسأله:

> آخر باك أب كان امتى؟

المفروض يقولك التاريخ والوقت.

أو افتح `github.com/bahr9/adam-backups` بنفسك — هتلاقي مجلد باسم تاريخ
النهاردة جواه ملفات JSON.

### الـ endpoints

لو غيّرت أي سر: خلي حد يبعت رسالة لعين الخبير على واتساب، وبعدها اسأل آدم:

> وريني آخر أسئلة العملاء

لو الرسالة ظهرت → Make.com شغال صح مع السر الجديد.

### الـ indexes

من تليجرام:

> دوّر في ملاحظاتك على أي حاجة

لو رجّع نتايج من غير خطأ → الـ index شغال.

---

## لو حاجة وقعت

| المشكلة | السبب الأرجح | الحل |
|---|---|---|
| الـ backup بيقول ❌ 401 | التوكن غلط أو منتهي | اعمل توكن جديد (خطوة 1‑ب) |
| الـ backup بيقول ❌ 404 | اسم الريبو غلط | اتأكد إن `GITHUB_REPO` = `bahr9/adam-backups` بالظبط |
| الـ backup بيقول ❌ 403 | التوكن مالوش صلاحية الكتابة | راجع إن **Contents** = Read and write |
| عين الخبير وقف بعد التغيير | Make.com لسه بالسر القديم | خطوة 2‑ج |
| الملاحظات لسه بتفشل | الـ index لسه Building | استنى وشوف حالته في الكونسول |

---

## ملخص: إيه بيتحط فين

| القيمة | Railway (الإنتاج) | `.env` (جهازك) | Make.com | Firebase Console |
|---|---|---|---|---|
| `GITHUB_TOKEN` | ✅ إجباري | اختياري | — | — |
| `GITHUB_REPO` | ✅ إجباري | اختياري | — | — |
| `AGENT_TASKS_SECRET` | ✅ إجباري | اختياري | ✅ نفس القيمة | — |
| `EYE_EXPERT_SECRET` | ✅ إجباري | اختياري | ✅ نفس القيمة | — |
| الـ 3 indexes | — | — | — | ✅ |

**القاعدة:** الإنتاج بيقرا من **Railway** بس. ملف `.env` بتاع جهازك
للتجربة المحلية فقط ومالوش أي تأثير على آدم الشغال.
