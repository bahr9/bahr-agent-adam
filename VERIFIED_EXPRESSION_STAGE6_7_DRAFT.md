# Verified Expression Layer — Stage 6 + 7 (DRAFT — القرارات الجوهرية مقفولة)
تاريخ: 2026-07-24
الحالة: **مسودة تصميم. مفيش كود اتلمس لسه.** لكن القرارات في قسم 0 و 3 و 4.1
**مقفولة نهائيًا** (اعتماد أحمد 2026-07-24) -- مش قابلة لإعادة نقاش في مراحل جاية.
باقي التفاصيل (نصوص القاموس، تكرار الـ job، إلخ) لسه مفتوحة للضبط وقت التنفيذ.
المرجع: [SELF_STATE_ENGINE_STAGE5.md](SELF_STATE_ENGINE_STAGE5.md)

---

## 0. الإطار الحاكم

**المبدأ السادس:** "كلما زادت مبادرة آدم في التعبير، وجب أن تقل حرية اللغة وتزيد صرامة الدليل."

ده مش مجرد إرشاد -- ده بيتترجم لقرار بنيوي **مقفول نهائيًا**:

> **Active Expression = صفر توليد لغوي خالص. Behavior آلي بحت -- الكود بيقرر ويبعت الجملة الجاهزة من القاموس المقفول مباشرة، مفيش أي "تأليف" في اللحظة دي، ومفيش LLM في مسار التركيب أصلًا.**
> **Passive Expression = الـ LLM مسموح يشتغل، لكن جوه حدود القاموس المقفول بس -- بيختار من جمل جاهزة، مش بيؤلف جملة جديدة. مع استمرار Evidence Trace + Backend Validation في الحالتين.**

**ده السؤال المحوري في المشروع كله، وخلاص اتقفل. مفيش رجوع ليه في أي مرحلة قادمة.**

التفاصيل تحت توضح ليه الاختيار ده مش تعسفي -- هو النتيجة المنطقية الوحيدة لو أخدنا "زادت المبادرة = قلّت الحرية" لآخرها.

---

## 1. القاموس المقفول (Closed Vocabulary)

### 1.1 البنية

سجل ثابت في الكود (مش prompt، مش قابل للتعديل من الموديل)، مفتاحه `(dimension, level, mode)`:

```
CLOSED_VOCABULARY = {
    (dimension: str, level: str, mode: "passive"|"active"): template: str
}
```

كل `template` نص فيه `{count}` أو placeholders محددة بس -- مفيش نص حر. الموديل (أو الـ backend job للـ Active) بيملأ الـ placeholders بقيم حقيقية من الـ Self-State المحسوبة، مش بيؤلف الجملة.

### 1.2 مقترح مبدئي للأبعاد التلاتة الحالية (نص للمراجعة، مش نهائي)

| dimension | level | mode | نص مقترح |
|---|---|---|---|
| unresolved_conflict | none | passive | "مفيش أي تعارض معلّق دلوقتي." |
| unresolved_conflict | elevated | passive | "عندي {count} تعارض غير محلول محتاج مراجعتك." |
| unresolved_conflict | high | passive | "عندي {count} تعارضات غير محلولة، وده رفع حالة المراجعة." |
| unresolved_conflict | high | **active** | "تنبيه: ظهر تعارض غير محلول يحتاج مراجعتك." |
| pending_obligation_load | none | passive | "مفيش أي قسط متأخر عن ميعاده دلوقتي." |
| pending_obligation_load | light | passive | "فيه {count} قسط متأخر عن ميعاده، محتاج مراجعة." |
| pending_obligation_load | concern | passive | "فيه {count} أقساط متأخرة عن ميعادها، محتاجة متابعة قريبة." |
| pending_obligation_load | concern | **active** | "تنبيه: {count} أقساط متأخرة عن ميعادها." |
| tracking_stability | none | passive | "معدل التصحيحات في آخر 30 يوم عادي." |
| tracking_stability | frequent_corrections | passive | "فيه {count} تصحيحات صريحة في آخر 30 يوم -- ملاحظة موضوعية، مش مؤشر مشكلة." |
| tracking_stability | frequent_corrections | **active** | **مفيش خالص** -- قرار Stage 5.1: البُعد ده متسحبش أبدًا من `HIGHEST_TIER`، فمستحيل يوصل لمسار Active من الأساس. |

لاحظ الفرق المتعمّد بين نص الـ passive والـ active لنفس المستوى (`high`/`concern`): الـ active أقصر، بلا أي صيغة شخصنة ("عندي")، وبصيغة تنبيه مباشرة -- بالظبط زي مثالك.

### 1.3 حالة `verified=false` -- قاموس مقفول برضه

لو الحساب فشل أو الدليل ناقص، الرد **لازم يكون من قاموس مقفول منفصل كمان**، مش نص حر:

```
VERIFIED_FALSE_VOCABULARY = {
    dimension: "مش قادر أتأكد من {dimension_label} دلوقتي -- المعلومة ناقصة.",
}
```

مفيش "تخمين لطيف" مسموح هنا خالص -- لو مفيش دليل، الرد الوحيد المسموح هو وصف النقص، بالظبط زي القاعدة الحاكمة الأصلية.

### 1.4 قاعدة التركيب (لو أكتر من بُعد اتسأل عنه مرة واحدة)

مفيش تأليف جملة جديدة تدمج الأبعاد. كل بُعد بيطلع كجملة منفصلة من القاموس، والجمل بتتلزّق ورا بعض بفاصل بسيط (سطر جديد أو نقطة). الموديل ممنوع يلخّص أو يعيد صياغة الاتنين في جملة واحدة مركّبة.

---

## 2. Evidence Trace -- `expression_id → state_id → evidence_event_ids`

### 2.1 المشكلة اللي بيحلّها

Self-State (Stage 5) بتتحسب "on the fly" ومش متخزنة -- ده قرار صح للحساب العادي (مفيش drift)، لكنه معناه لو حد سأل بعد شهر "ليه آدم قال كده يوم كذا؟"، مفيش snapshot نرجعله. لازم *لحظة التعبير نفسها* -- مش كل حساب -- تتجمّد وتتسجل.

### 2.2 التصميم المقترح -- 3 مستويات مربوطة

```
StateSnapshot (تتسجل بس وقت ما فعليًا هيتم تعبير)
  { state_id, computed_at, dimensions: {الـ dict الكامل من compute_self_state()} }

Expression (تتسجل كل مرة نص من القاموس فعليًا اتبعت لأحمد)
  { expression_id, state_id, dimension, level, mode,
    template_key, rendered_text, verified: bool,
    sent_at, chat_id }

evidence_event_ids -- موجودة أصلاً جوه StateSnapshot.dimensions[dim].evidence_event_ids
(من Stage 5) -- مفيش داعي نكررها في Expression نفسها، بنشاور عليها بس.
```

المسار الكامل للتتبع: `expression_id` → `Expression.state_id` → `StateSnapshot.dimensions[dim].evidence_event_ids` → `adam_events` (الحقيقة الأصلية). 3 خطوات، كل واحدة قابلة للتحقق مستقلة.

### 2.3 مين بيكتب إيه، وإمتى

- **StateSnapshot**: بتتسجل **بس** لما فيه نية حقيقية إن تعبير هيحصل (مش كل قراءة عادية) -- يعني جوه الـ Gate نفسه (قسم 3)، مش جوه `self_state_engine.py`. ده بيمنع تضخم غير ضروري (مفيش snapshot لكل مرة حد بيسأل loan_overview العادية مثلًا).
- **Expression**: بتتسجل بعد الإرسال الفعلي (أو محاولة الإرسال) -- بما فيها حالة `verified=false` (عشان حتى "مش قادر أتأكد" ليها سجل).

---

## 3. الحاجز الفعلي في الـ Backend

**تصنيف نهائي مقفول (اعتماد أحمد 2026-07-24):** الضمان الرسمي = 3 آليات **حتمية 100%** بس. أي حاجة تانية = دفاع إضافي (Defense in Depth)، مفيدة لكن **مش** جزء من الضمان الرسمي ومش هيتم الاعتماد عليها لوحدها.

### الضمان الرسمي (Deterministic -- 3 آليات، دي اللي بنعتمد عليها فعليًا)

**1. Information Containment (الأقوى، بنيوية)**
الموديل **معندوش وصول مباشر** لأي رقم أو مستوى خام من Self-State أبدًا. مفيش tool اسمه "get_unresolved_conflict_count" أو أي حاجة تسرّب الأرقام الخام. الطريقة الوحيدة اللي الموديل يعرف بيها أي حاجة عن حالته هي عبر **Gate function** واحدة (`request_verified_expression(dimension, mode)`) بترجع نص جاهز من القاموس المقفول بس -- مش بيانات خام يقدر يبني منها جملة لوحده. مينفعش الموديل "يخترع" رقم غلط، لأنه أصلاً معندوش وصول للأرقام الحقيقية إلا من خلال النص الجاهز.

**2. Verbatim Match Validator (حتمي، قابل للاختبار الكامل)**
لما الموديل يستخدم النص الراجع من الـ Gate، الـ backend بيتأكد إن الرسالة النهائية اللي هتتبعت لأحمد **فيها نفس النص بالحرف** (substring match) من غير أي تعديل. لو الموديل حاول "يحسّن الصياغة" أو يلخّص، الـ match بيفشل → الرسالة **بترفض ترسل** بشكلها الحالي، والنظام يرجع للنص الأصلي من القاموس حرفيًا (fallback آلي، مش طلب من الموديل يعيد المحاولة). هي "حتمية" بالمعنى الدقيق: نفس المدخل بيدي نفس القرار (match/no-match) كل مرة، زي أي دالة رياضية -- ده اللي بيخليها قابلة للاختبار الشامل (تفاصيل الاختبار في قسم 3.1 تحت).

**3. Evidence Trace (قسم 2 فوق)**
كل تعبير مربوط بـ `expression_id → state_id → evidence_event_ids` قابلة للتحقق مستقل، حتمية بالكامل (استعلام Firestore مباشر، مفيش تخمين).

### دفاع إضافي (Defense in Depth -- مش جزء من الضمان الرسمي)

**Heuristic Pre-Send Scanner:** سكانر بسيط (قائمة كلمات/تعبيرات دالة على حالة داخلية بالعربي: "حاسس"، "قلقان"، "متضايق"، "مرتاح"، "حالتي النفسية"، إلخ) بيفحص **أي** رسالة خارجة لأحمد. لو لقى لغة من النوع ده **من غير** ما يكون فيه `request_verified_expression` اتنادى في نفس الدورة، الرسالة بتتوقف وتترسل نسخة بديلة (أو تتسجل كـ حادثة للمراجعة).

**ليه مش ضمان رسمي:** الفحص هنا "احتمالي/تخميني" (heuristic) -- اللغة الطبيعية أوسع من أي قائمة كلمات، فمفيش ضمان رياضي إنه هيمسك كل حالة. طبقة حماية زيادة حقيقية (كود فعلي، مش مجرد تعليمة)، لكن **متبقاش في نفس مرتبة الآليات التلاتة الحتمية فوق**.

**System Prompt:** قيد صريح ("ممنوع تصف حالتك الداخلية بأي صياغة حرة...") -- طبقة ناعمة مكمّلة زيادة، نفس منطق الـ Scanner (مفيدة، مش ضمان).

### 3.1 سؤال متابعة مسجّل لمرحلة "مراجعة التنفيذ" (مش دلوقتي)

سؤالك عن تغطية اختبار الـ Verbatim Validator محفوظ هنا رسميًا: وقت التنفيذ الفعلي، الاختبار المطلوب مش عيّنة عشوائية -- بما إن القاموس **مقفول ومحدود العدد** (جدول ثابت من `(dimension, level, mode)`)، التغطية الكاملة معناها اختبار **كل مدخل في القاموس فعليًا** (كل صف في جدول 1.2 + `VERIFIED_FALSE_VOCABULARY`) بسيناريوهين لكل واحد: (أ) نص مطابق بالحرف → لازم يعدي، (ب) نص فيه أي تغيير طفيف (كلمة إضافية، ترتيب مختلف، إلخ) → لازم يترفض. عدد الاختبارات = 2 × عدد صفوف القاموس بالظبط -- رقم محدود ومعروف مقدمًا، مش عيّنة. هنرجع للنقطة دي بالتفصيل وقت التنفيذ.

---

## 4. Policy منفصلة: Active مقابل Passive

### 4.1 القرار البنيوي -- **مقفول نهائيًا (اعتماد أحمد 2026-07-24)، مفيش رجوع ليه في أي مرحلة قادمة**

**Active = Behavior آلي بحت، صفر توليد لغوي.** مش "الموديل بيقرر يبادر"، ده **مفيش موديل في الصورة خالص**. الكود بيقرر (عبر `decision_engine` الموجود من Stage 5) ويبعت الجملة الجاهزة من القاموس المقفول مباشرة -- بدون أي مرور على LLM. بدل ما نعتمد على الموديل "يقرر يبادر"، الـ Active بيبقى **backend job مجدول** (زي `check_loans_job` الموجود فعلًا في `main.py`) بيعمل:

```
كل فترة (مثلًا كل ساعة):
  self_state = self_state_engine.compute_self_state()
  decisions = decision_engine.decide_expression(self_state)
  لكل بُعد فيه mode == "active":
      لو الشروط الإضافية (تحت) اتحققت:
          text = render_from_closed_vocabulary(dim, level, mode="active")
          سجّل StateSnapshot + Expression
          ابعت النص للـ chat مباشرة -- من غير ما يمر على LLM خالص
```

**ده مش تبسيط، ده تنفيذ حرفي للمبدأ السادس:** أعلى درجة مبادرة (Active) = أقل حرية لغوية ممكنة (صفر -- مفيش LLM في المسار أصلاً). الموديل ممكن يتكلم عن الحدث لاحقًا لو أحمد سأل عليه (Passive عادي)، لكن التنبيه المبدئي نفسه بيطلع من الكود مباشرة.

### 4.2 الشروط الإضافية قبل ما Active يبعت فعليًا (كل الشروط دي، مش أي واحدة)

1. **Severity**: مستوى `high`/`concern` بس (أصلاً مضمون من `decision_engine.HIGHEST_TIER`).
2. **Evidence أقوى**: `evidence_event_ids` مش فاضية، **و** كل event id فيها بيترجع فعلي من `event_store.get_event()` وقت الإرسال (recheck نهائي، مش الاعتماد على قيمة محسوبة من شوية).
3. **Confidence كامل / مفيش غموض**: الحساب نفسه ما واجهش أي خطأ (Firestore fail جزئي، إلخ) -- `self_state_engine` المقترح يرجع فلاج `computation_ok: bool` لكل بُعد.
4. **Cooldown**: مضمون بنيويًا من `decision_engine` الموجود (مبيكررش على نفس المستوى، بيرجع يشتغل بس لو نزل وطلع تاني) -- مفيش داعي cooldown زمني إضافي فوقه في v0.1.
5. **الأسلوب**: نص Active منفصل تمامًا عن نص Passive لنفس المستوى (قسم 1.2) -- أقصر، بلا شخصنة، تنبيهي.
6. **بدون لغة عاطفية موسّعة**: مضمون بنيويًا لأن النص كله جاي من القاموس المقفول -- مفيش مساحة الموديل يزوّد فيها حاجة أصلًا (مفيش LLM في المسار).

### 4.3 Passive Policy (أخف، لكن برضه محكومة -- ومقفولة هي كمان)

- بتتفعّل لما الموديل يكتشف سؤال حقيقي من أحمد (نية واضحة -- "عامل إيه"، "فيه حاجة محتاجة متابعة"، سؤال مباشر عن الأقساط).
- الموديل بينادي `request_verified_expression(dimension, mode="passive")` لكل بُعد relevant.
- مسموح لأي مستوى (مش بس أعلى مستوى) -- لأن أحمد سأل، فالصدق يقتضي وصف الحالة الحقيقية أيًا كانت.
- **الموديل بيختار من جمل جاهزة، مش بيؤلف جملة جديدة** -- الحرية الوحيدة المسموحة هي تغليف النص الجاهز بمقدمة/خاتمة طبيعية ("تمام، بالنسبة للأقساط دلوقتي:")، لكن **ممنوع** يلمس أو يعيد صياغة النص المحسوب نفسه (بيتضمن بالـ Verbatim Match Validator، قسم 3).

---

## 5. حالة القرارات

### مقفول نهائيًا (اعتماد 2026-07-24 -- مفيش رجوع ليه)
- ✅ Active = Behavior آلي بحت، صفر LLM، صفر توليد لغوي.
- ✅ Passive = LLM يختار من القاموس المقفول، مبيؤلفش.
- ✅ الضمان الرسمي = 3 آليات حتمية بس (Information Containment, Verbatim Match Validator, Evidence Trace). الـ Heuristic Scanner والـ System Prompt = دفاع إضافي، مش جزء من الضمان الرسمي.
- 📌 سؤال تغطية اختبار الـ Verbatim Validator محفوظ لمرحلة مراجعة التنفيذ (قسم 3.1).

### لسه مفتوحة للضبط وقت التنفيذ (مش قرارات جوهرية، تفاصيل تنفيذية)
1. **صياغة القاموس** (قسم 1.2) -- الأمثلة نقطة بداية، قابلة للتعديل وقت الكتابة الفعلية.
2. **الـ Heuristic Scanner** (دفاع إضافي، مش ضمان رسمي) -- يتبني من الأول ولا يتأجل زي `tracking_stability`؟
3. **تكرار الـ Active job**: كل ساعة كفاية، ولا فترة تانية؟
4. **`computation_ok` flag**: تعديل صغير مقترح على `self_state_engine.py` عشان "مفيش غموض" -- موافق نضيفه؟
