# UI Channel V1 — قناة adam-ui (الشريحة الأولى)

**التاريخ:** 2026-08-07
**العقد المرجعي:** `adam-ui/docs/execution/CONTRACT_V1.md` (وسم `contract-v1-freeze`)
**الحالة:** الشريحة الأولى (أصغر إثبات) شغالة ومختبرة — 11/11 اختبار.

## إيه اللي اتبنى

قناة إضافية جنب تليجرام بتكلم واجهة adam-ui بنص العقد المجمّد:

| | |
|---|---|
| `GET /ui/events` | SSE — كل رسالة = حدث JSON واحد في حقل `data` (UTF-8 حرفي، camelCase) |
| `POST /ui/command` | أمر JSON واحد، الرد `202` = "استُلم"، النتيجة أحداث على الـ SSE |
| الأمان | `Authorization: Bearer <ADAM_TOKEN>` على كل طلب. توكن غلط/غايب = `401` بدون تفاصيل. `ADAM_TOKEN` نفسه مش متظبط = `503` fail-closed (قاعدة أوديت 2026-08-04) |

**متغير بيئة جديد مطلوب على Railway:** `ADAM_TOKEN` (نفس القيمة على جانب الواجهة).

## الملفات

- **`services/ui_channel.py`** (جديد) — كل القناة: event bus (نشر لكل مشتركي الـ SSE)،
  إدارة الجولات (turnId، الاستبدال، الإلغاء)، خطافات الأدوات، بطاقة الأقساط،
  والـ Flask Blueprint. مفيش أي منطق أعمال هنا — القراءة من نفس `loan_service`.
- **`services/claude_service.py`** — سطرين حوالين `_execute_tool` في لوب الأدوات
  ينادوا `_ui_notify_tool_started/_ui_notify_tool_finished`: استيراد lazy، كله جوه
  `try`، وno-op تام لو الثريد مش شغّال جولة UI — يعني **كل رسائل تليجرام بتعدي
  من غير أي أثر**.
- **`main.py`** — تسجيل الـ blueprint جوه `try` (فشل التسجيل = لوج + تليجرام يكمل
  عادي). `_ui_runtime_call` بيستخدم **نفس بايبلاين تليجرام بالظبط**:
  Runtime → EB → `verified_expression` — الرد الواحد حاجة واحدة مهما كانت القناة.
- **`test_ui_channel.py`** (جديد) — 11 اختبار، صفر شبكة وصفر إنتاج (runtime مزيف +
  قراءة أقساط مزيفة)، ومن غير استيراد `main.py` خالص.

## تسلسل الجولة (زي ما الاختبارات بتثبته حرفيًا)

```
POST {"type":"text","text":"راجع الأقساط المتأخرة"}  → 202
SSE:
  orb.state thinking (turnId)
  orb.state executing + tool.started / tool.succeeded   ← من خطافات لوب الأدوات، لايف
  stage.push (installments, workspace, evidence + جدول selectableBy)   ← قبل البث (عرف التأليف، قسم 6)
  orb.state speaking + stream.start / delta* / end       ← deltas متجمعة = النص حرفيًا
  orb.state warning|success   ← warning بس لو فيه قسط متأخر فعليًا (القلق بالحالة مش الموضوع)
  turn.completed
```

قواعد العقد الصلبة المطبقة:

- **`warning` ≠ `error`**: متأخرات حقيقية = `warning` (بتفضل بعد `turn.completed`)،
  و`error` للعطل التقني فقط (استثناء في الـ runner).
- **الاستبدال (supersede)**: أمر جديد وجولة شغالة = القديمة تتقفل بـ `turn.completed`
  (المقاطعة فعل مقصود)، وقفل الجولة بيضمن إن **ولا حدث** بالـ turnId القديم يطلع
  بعد الإغلاق — حتى لو الـ thread القديم لسه شغال (بيكتشف ويسكت عند أقرب checkpoint).
- **`cancel` الصريح** = الاستثناء الوحيد اللي بيقفل بـ `turn.failed`.
- **بطاقة واحدة `evidence`** بفصل `direct`/`derived` في الحقائق — ممنوع أنواع باسم الكيانات.

## حدود الشريحة دي (مش نواقص مخفية)

1. **البث تقطيع للرد النهائي** (chunking) مش token streaming من الموديل — التسلسل
   والعقد محترمين، والترقية لبث حقيقي تعديل داخلي في `ui_channel` مش خرق عقد.
2. **أوامر `intent`/`confirm` مؤجلة**: بتترد بـ `202` + حدث `turn.failed` فوري بسبب
   واضح و`retryable: false` — رفض مؤدب بأحداث حقيقية مش صمت. دي بوابة الشريحة
   الجاية (دورة الـ `confirmation` للأفعال الخطرة).
3. **Morning Brief `displayItems`** (قسم 6) لسه — بعد ما الأساس ده يثبت مع الواجهة
   الحقيقية، الـ adapter هيتبني على نفس `stage.push` الموجود.
4. بطاقة الأقساط بتتبني لو أي أداة `loan_*` اتنفذت في الجولة — تعميم الخريطة
   (مشاريع، طقس، مهام) بييجي مع دمج البريف.
5. `تنبيه تشغيلي`: الجولة الملغية/المستبدلة أحداثها بتتكتم فورًا، لكن نداء Claude
   الجاري مبيتقطعش في النص (بيكمل في الخلفية ويترمي) — وفر التوكنز هنا تحسين
   مستقبلي لو بان إنه محتاج.

## تشغيل الاختبارات

```bash
./.venv/Scripts/python.exe test_ui_channel.py
```

وبوابة القبول الحقيقية (C2 في العقد) بتتشغّل من جانب الواجهة ضد آدم الحي بعد ضبط
`ADAM_TOKEN` — نفس رحلة الـ mock بالظبط.
