# ⚠️ تحذير مؤقت لمستخدمي Bahr OS — الفواتير

**التاريخ:** 2026-08-05 · **الحالة:** الباگ لسه موجود في النسخة الشغالة

---

## نص جاهز للإرسال لأي حد بيستخدم Bahr OS

> **مهم — قبل ما تعمل أي فاتورة:**
>
> فيه مشكلة في النسخة الحالية من Bahr OS: لما تعمل فاتورة، بنود الأتعاب
> ممكن تتسحب من **آخر مشروع فتحته في نفس التبويب** بدل المشروع اللي
> بتعمله فاتورة فعلاً.
>
> **يعني إيه عمليًا:** لو فتحت مشروع "أ"، وبعدين رحت لمشروع "ب" وعملتله
> فاتورة — الفاتورة ممكن تطلع ببنود مشروع "أ" وأسعاره.
>
> **الحل المؤقت لحد ما ننشر التحديث:**
>
> 1. **اقفل التبويب وافتحه من جديد** قبل ما تعمل فاتورة لأي مشروع.
> 2. افتح **المشروع المطلوب بس** — متفتحش غيره قبله في نفس التبويب.
> 3. بعد ما الفاتورة تطلع، **راجع البنود سطر سطر** وتأكد إنها فعلاً
>    بتاعة المشروع ده قبل ما تعتمدها أو ترسلها لحد.
>
> **الفواتير القديمة:** بنراجعها كلها دلوقتي، وهنقولك أنهي فواتير
> محتاجة تصحيح. متعتمدش على أي فاتورة قديمة في حسابات نهائية لحد ما
> المراجعة تخلص.

---

## التفاصيل التقنية (لأحمد، مش للإرسال)

### الباگ

في `public/index.html` (النسخة المنشورة)، دالة `getLaborData`:

```js
const data = JSON.parse(localStorage.getItem(key) || '{"items":[]}');
```

بتقرا بنود الأتعاب من `localStorage` بالمفاتيح `bahrFoundationData` /
`bahrFinishData` / `bahrFinalData` — واللي بتتكتب فيها بيانات **آخر
مشروع اتفتح في المتصفح**، مش المشروع اللي بيتعمله فاتورة.

### الإصلاح

موجود في `BAHR_OS_v2.html` بس، **ومش منشور**:

```js
const phaseItems = (p) => ((projectData && projectData[`${p}data`] &&
    projectData[`${p}data`].items) || []);
```

بيقرا من مستند المشروع نفسه. الملفين مختلفين في **514 سطر**، والمراجعة
لسه ماتمتش.

### النطاق

**كل فاتورة اتعملت لحد النهاردة** عدّت على المسار الباگ. ده مش معناه إن
كلها غلط — لو المستخدم كان فاتح المشروع الصح، البيانات طلعت صح بالصدفة.
لكن مفيش ضمانة في أي فاتورة.

### التحديد

بيتم من التصدير الكامل (متجدول 2026-08-06 الساعة 10:10 صباحًا): مقارنة
`invoices.items[].name` بأوصاف `{phase}data.items[].desc` لنفس المشروع.
عدم التطابق = دليل إن الباگ اشتغل.

النتيجة هتتحدد على `invoices.needs_review` وقت الاستيراد لـ Supabase.

### ملحوظة على الحل المؤقت

قفل التبويب بيمسح الـ `localStorage`؟ **لأ** — `localStorage` بيفضل بعد
قفل التبويب وحتى بعد قفل المتصفح. الخطوات فوق بتقلل الخطر (لأن فتح
المشروع الصح بيكتب فوق المفاتيح)، بس **مابتلغيهوش**.

الضمانة الوحيدة الأكيدة هي **مراجعة بنود كل فاتورة يدويًا** قبل
الاعتماد، لحد ما v2 تتنشر.
