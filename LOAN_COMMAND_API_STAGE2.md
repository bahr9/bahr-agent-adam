# Loan Command API — Stage 2
تاريخ: 2026-07-24
الحالة: **منفّذ ومتحقق منه فعليًا** ضد Firestore الحقيقي.
المرجع السابق: [AUDIT_REPORT_STAGE0.md](AUDIT_REPORT_STAGE0.md), [EVENT_SCHEMA.md](EVENT_SCHEMA.md)

---

## 1. اللي اتغيّر

| قبل | بعد |
|---|---|
| `loan_mark_paid` (أداة واحدة، كتابة مباشرة، بدون تسجيل حدث) | 3 أدوات: `loan_record_installment`, `loan_update_installment`, `loan_resolve_conflict` — كلها بتمر إجباريًا عبر Event Store |
| `loan_service.mark_installment_paid()` / `set_paid()` | **اتشالوا نهائيًا** (كود ميت بعد نقل الكتابة) |
| `firebase_service.save_loan_paid_status()` | **اتشالت نهائيًا** (نفس السبب) |
| الكتابة والحدث (لو كانا موجودين) كانوا هيبقوا كتابتين منفصلتين | كتابة واحدة **ذرية** (Firestore batch) عبر `event_store.record_event_with_write` |

`loan_mark_paid` مش موجودة في `TOOLS` ولا في `_execute_tool()` خالص دلوقتي — مفيش مسار كتابة قديم فاضل ممكن يتنادى بالغلط.

---

## 2. نقطة الاعتراض

زي ما اتفقنا: الـ enforcement مش جوه `_execute_tool()` نفسها (مجرد سطر حر في الـ dispatch)، ده جوه **الدالة نفسها** في [services/loan_commands.py](services/loan_commands.py):
كل دالة (`loan_record_installment`, `loan_update_installment`, `loan_resolve_conflict`) بتنادي `event_store.record_event_with_write()` كخطوة أولى وإجبارية *قبل* أي كتابة فعلية، جوه دالة `_commit()` المشتركة بينهم. الضمانة دي بتتنقل مع الدالة نفسها بغض النظر مين هينادي عليها مستقبلًا (Observer، scheduler، أي حاجة).

`_execute_tool()` دلوقتي مجرد routing بحت للثلاث أدوات دول — زي أي أداة تانية.

---

## 3. Atomicity — إضافة جوهرية عن التصميم الأصلي

أثناء التنفيذ، لاحظنا إن استخدام كتابتين منفصلتين (سجّل الحدث، وبعدين اكتب الـ domain data) بيفتح ثغرة: لو حصل خطأ بين الكتابتين، ممكن يتسجل حدث بيوصف تغيير ما حصلش فعليًا، أو العكس. ده بيبطل نفس الضمانة اللي بنبنيها.

الحل: **`event_store.record_event_with_write()`** (إضافة لملف Stage 1) — بتستخدم `firestore.batch()` عشان تكتب الحدث + الـ domain document في **commit واحد ذري**: إما الاتنين بيحصلوا سوا، أو ولا واحد فيهم. أي Command API جديد (مصاريف/مشاريع لاحقًا) لازم يستخدم الدالة دي مش كتابتين منفصلتين.

---

## 4. باگ حقيقي اتكتشف واتصلح أثناء التحقق (مهم)

أول تشغيل فعلي للتحقق كشف باگ: الكتابة كانت بتستخدم مفتاح نصي بنقطة `{f"paid.{key}": paid}` مع `batch.set(..., merge=True)`. اتضح إن Firestore **مبيفكّش النقطة كـ path متداخل في `.set()`** (سواء جوه batch أو لأ) — بيعتبرها اسم حقل حرفي فيه نقطة. النتيجة: القسط بيتسجل في حقل منفصل زي `"paid.ca_71"` بدل ما يتحدّث جوه خريطة `paid` الحقيقية اللي كل الكود بيقرأ منها.

**الأهم:** ده نفس النمط اللي كانت مستخدمة فيه `save_loan_paid_status` **القديمة** (قبل ما نمسحها في المرحلة دي). يعني على الأغلب كانت نفس المشكلة موجودة من الأساس في `loan_mark_paid` القديمة — أي محاولة قديمة لتعليم قسط كمدفوع غالبًا ماكانتش بتنعكس صح في `loan_overview`. فحصنا الوثيقة الفعلية في Firestore ولقيناها فاضية تمامًا قبل اختبارنا (متسقة مع لوج قديم كان بيورّي "إجمالي المدفوع: 0 جنيه") — يعني مفيش بيانات حقيقية اتفقدت، بس الميزة القديمة على الأرجح ماكانتش بتشتغل صح من الأساس.

**الإصلاح:** استخدام dict متداخل حقيقي `{"paid": {key: paid}}` بدل المفتاح النصي بالنقطة. `merge=True` مع dict متداخل بيعمل deep-merge صحيح موثّق في Firestore.

**أثر جانبي غير مؤذي متبقي:** فيه حقل تالف واحد لسه موجود في وثيقة `loans/paid_status` من التشغيلة الأولى قبل الإصلاح — اسمه حرفيًا `paid.ca_71` (بنقطة في الاسم). **مفيش كود بيقرأه** (كل القراءة بتدور على حقل اسمه `paid` بالظبط)، فمالوش أي تأثير وظيفي. اتفقنا يتسيب دلوقتي وينتظف لاحقًا (يدويًا من Firebase console، أو كخطوة صغيرة في نهاية المرحلة).

---

## 5. الفرق بين الثلاث أدوات

- **`loan_record_installment`**: الاستخدام العادي — تسجيل حالة دفع لأول مرة.
- **`loan_update_installment`**: تصحيح قيمة كانت متسجلة قبل كده — **سبب إجباري** (`reason`)، بيترفض تمامًا من غيره.
- **`loan_resolve_conflict`**: تطبيق قرار يدوي من أحمد لحل تعارض بين ادعاءين متضاربين — **سبب إجباري** كمان، وبيتسجل بعلامة مميزة `source="manual_resolution"` في الـ Event Store عشان يتفرّق عن الأحداث العادية. لسه مفيش Observer (مرحلة 3) بيكتشف تعارضات تلقائيًا، فالدالة دي بنية جاهزة مش مستخدمة فعليًا لحد ما مراحل 3/4.

---

## 6. التحقق الفعلي (Verification)

اتعمل ضد Firestore الحقيقي، على قسط بعيد جدًا وآمن (Credit Agricole، آخر قسط 01/06/2032، كان `False` أصلاً) — قسط بعيد عن أي تتبع فعلي حالي لأحمد:

1. ✅ رفض `loan_record_installment` لبرنامج غير موجود.
2. ✅ رفض `loan_update_installment` من غير `reason`.
3. ✅ `loan_record_installment` كتب فعليًا (False → True)، والقراءة العادية (`loan_service.is_paid`) عكست القيمة الجديدة صح.
4. ✅ الحدث المسجل فيه كل الحقول المتوقعة (`entity`, `attribute`, `previous_value`, `new_value`, `source`, `actor`).
5. ✅ `loan_update_installment` رجّع القيمة لأصلها (True → False) بسبب موثّق، والقراءة عكست الرجوع صح.
6. ✅ حدث التصحيح فيه `reason` و `is_correction=True` في `metadata`.
7. ✅ `loan_mark_paid` مش موجودة في `TOOLS` خالص، والثلاث أدوات الجديدة موجودة.
8. ✅ القيمة النهائية للقسط رجعت لنفس حالتها الأصلية بالظبط (`False`).
9. 🧹 كل أحداث الاختبار (3 أحداث عبر التشغيلتين) اتمسحت من `adam_events` بعد التأكيد — الـ Store رجع نضيف من بيانات اختبار.

---

## 7. Definition of Done — مرحلة 2 (مقترح للاعتماد)

- [x] `loan_mark_paid` والكتابة المباشرة القديمة اتشالوا نهائيًا (كود + tool definition + dispatch).
- [x] الأدوات الثلاثة الجديدة موجودة وموثّقة، وموجّهة عبر `_execute_tool()`.
- [x] كل كتابة لازم تسجل حدث في نفس الـ commit الذري (`record_event_with_write`) — مفيش مسار كتابة بدون حدث.
- [x] `loan_update_installment` و `loan_resolve_conflict` بيرفضوا فعليًا من غير `reason`.
- [x] تحقق فعلي (مش نظري) ضد Firestore الحقيقي، بما فيه اكتشاف وإصلاح باگ حقيقي في آلية الكتابة (nested merge).
- [x] بيانات الاختبار كلها اتنظفت من `adam_events`.
- [ ] تنظيف الحقل التالف `paid.ca_71` من `loans/paid_status` (غير عاجل، غير مؤثر وظيفيًا).
- [ ] **قرار أحمد:** اعتماد المرحلة كـ "خلصت" والانتقال لمرحلة 3 (Loan Conflict Observer).
