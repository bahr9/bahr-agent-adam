# Conflict Resolution Flow — Stage 4
تاريخ: 2026-07-24
الحالة: **منفّذ ومتحقق منه فعليًا** ضد Firestore الحقيقي.
المرجع السابق: [LOAN_CONFLICT_OBSERVER_STAGE3.md](LOAN_CONFLICT_OBSERVER_STAGE3.md)

---

## 1. اللي اتغيّر

`loan_record_installment` ([loan_commands.py](services/loan_commands.py)) دلوقتي بتفحص **قبل** أي كتابة: لو فيه قيمة سابقة متسجلة فعليًا لنفس القسط ومختلفة عن القيمة المطلوبة، **بتوقف تمامًا**:
- مفيش حدث بيتسجل في `adam_events`.
- مفيش كتابة بتحصل في `loans/paid_status`.
- بترجع رسالة واضحة (⚠️ تعارض) توضح القيمة القديمة والجديدة، وتوجّه إن التغيير الفعلي محتاج `loan_resolve_conflict` بسبب موثّق.

القيمة اللي **مش** بتتعارض (نفس القيمة القديمة -- duplicate) بتعدي عادي من غير أي توقف.

`loan_update_installment` و `loan_resolve_conflict` **مبيوقفوش** -- السبب الإجباري اللي بيطلبوه هو نفسه التوثيق/التأكيد المطلوب، فمفيش داعي لطبقة توقف إضافية قبلهم.

---

## 2. "الوقف + التأكيد اليدوي" في سياق شات بوت -- إزاي بيتترجم عمليًا

النظام مش عنده UI حوار متزامن (Ahmed مش بيضغط "موافق/رفض" في نافذة). التوقف بيتترجم لتدفق محادثة طبيعي عبر الـ agentic loop الموجود أصلاً:

1. الموديل يحاول `loan_record_installment` → بيترفض ويرجع رسالة التعارض كـ tool result.
2. الموديل (حسب تعليمات صريحة جديدة في الـ system prompt) **لازم** يعرض التعارض على أحمد بوضوح (القيمة القديمة/الجديدة) ويسأله يقرر -- **ممنوع** يخمّن أو يقرر لوحده.
3. أحمد يرد بقراره (ورايح، غالبًا مع سبب أو سياق).
4. الموديل يستخدم `loan_resolve_conflict` بـ `reason` = نفس كلام أحمد -- مش سبب من اختراع الموديل.
5. لو أحمد لسه ما ردّش، الموديل ممنوع ينادي `loan_resolve_conflict` خالص -- الموضوع يفضل معلّق.

القاعدة دي اتضافت صراحة في الـ system prompt الثابت (static_part) في [claude_service.py](services/claude_service.py) عشان تبقى قيد حقيقي على سلوك الموديل، مش افتراض ضمني.

---

## 3. دفاع بطبقتين (Defense in Depth)

- **الطبقة الأساسية (Stage 4، جديدة):** فحص استباقي قبل الكتابة في `loan_record_installment` -- بيمنع التعارض من الأساس في الحالة العادية (طلبات متتالية، مش متزامنة).
- **الطبقة الاحتياطية (Stage 3، موجودة):** تصنيف بعد الكتابة في `_commit()` -- شبكة أمان لحالة نادرة جدًا (Race Condition): طلبين متزامنين تمامًا عدّوا الفحص الاستباقي في نفس اللحظة قبل ما أي حد يكتب. الطبقة دي **بتلاحظ وتسجّل بس**، ومش متوقعة تتفعل في التشغيل العادي بعد Stage 4.

مفيش تكرار وظيفي هنا -- كل طبقة بتغطي سيناريو مختلف.

---

## 4. تبسيط متعمّد عن التصميم الأصلي

التصميم الأصلي وصف الحالة كـ"ادّعاءين نشطين موثوقين (two active trusted claims) بقيم incompatible". النظام الحالي معندوش مفهوم "ادّعاءات معلّقة متعددة" منتظرة قرار -- كل كتابة ناجحة فورًا بتبقى "الحقيقة الحالية". عمليًا ده بيبسّط لـ: "آخر قيمة مؤكدة" مقابل "قيمة جديدة جاية من قناة أول تسجيل". ده كافي تمامًا لنطاق v0.1 (الأقساط)، وبناء نظام "ادّعاءات معلّقة متوازية" بمعزل حقيقي هيكون overengineering مش مطلوب دلوقتي.

---

## 5. التحقق الفعلي (Verification)

اتعمل ضد Firestore الحقيقي، نفس القسط الآمن (Credit Agricole، 01/06/2032):

1. ✅ تسجيل أول قيمة (True) عدى عادي.
2. ✅ محاولة تسجيل قيمة متعارضة (False) عبر `loan_record_installment` **اترفضت** (`ok=False`, `event_id=None`)، والرسالة فيها ⚠️ تعارض.
3. ✅ القيمة الفعلية في Firestore **متغيرتش** بعد الرفض -- تأكيد إن الرفض حقيقي مش شكلي.
4. ✅ **مفيش حدث جديد** اتسجل بسبب المحاولة المرفوضة (لسه حدث واحد بس في التاريخ).
5. ✅ إعادة تسجيل *نفس* القيمة (duplicate، مش تعارض) عدت عادي من غير توقف.
6. ✅ `loan_resolve_conflict` بسبب موثّق كتب فعليًا وغيّر القيمة، واترجعت لأصلها بالظبط.
7. ✅ الحدث الأخير اتصنف `update` من الـ Observer (مش conflict) -- القناة الصريحة اشتغلت صح.
8. 🧹 كل الـ 3 أحداث الاختبار اتمسحت بعد التأكيد.

---

## 6. Definition of Done — مرحلة 4 (مقترح للاعتماد)

- [x] `loan_record_installment` بتوقف فعليًا (مفيش حدث، مفيش كتابة) لو فيه تعارض حقيقي.
- [x] القيم غير المتعارضة (duplicate) بتعدي عادي من غير احتكاك زيادة.
- [x] `loan_resolve_conflict` مؤكد إنه المسار الوحيد لتجاوز التوقف، وبسبب إجباري موثّق.
- [x] الـ system prompt فيه قيد صريح يمنع الموديل يخمّن أو يقرر التعارض لوحده -- لازم يعرضه على أحمد ويستخدم سببه هو بالظبط.
- [x] دفاع بطبقتين موثّق (استباقي + شبكة أمان للـ race condition).
- [x] تحقق فعلي (مش نظري) ضد Firestore الحقيقي، بما فيه التأكد إن الرفض حقيقي (مفيش كتابة ولا حدث) مش مجرد رسالة شكلية.
- [x] بيانات الاختبار كلها اتنظفت.
- [ ] **قرار أحمد:** اعتماد المرحلة كـ "خلصت". هنا كمان نطاق الأقساط (Loans) v0.1 بيكتمل عمليًا (المراحل 1-4 كلها شغالة سوا) -- الانتقال بعد كده لمرحلة 5 (Self-State Engine v0.1) هو أول استخدام حقيقي للأحداث دي في بناء حالة داخلية.
